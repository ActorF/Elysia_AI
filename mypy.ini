[mypy]
# Local model runtimes are downloaded into this Git-ignored boundary. They are
# third-party distributions, not Elysia AI source, and often contain duplicate
# top-level module names that cannot share one mypy invocation.
exclude = ^models[\\/]cache[\\/]
